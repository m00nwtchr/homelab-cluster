---
apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: immich-backup
  namespace: media
spec:
  cluster:
    name: immich-postgres
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
