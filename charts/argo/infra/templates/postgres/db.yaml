apiVersion: "acid.zalan.do/v1"
kind: postgresql
metadata:
  name: postgres-cluster
spec:
  teamId: "acid"
  volume:
    size: 20Gi
  numberOfInstances: 1
  users:
{{/*    zalando: # database owner*/}}
{{/*      - superuser*/}}
{{/*      - createdb*/}}

    openwebui: [ ]

  databases:
    openwebui: openwebui
  postgresql:
    version: "17"
