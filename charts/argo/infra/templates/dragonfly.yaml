apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dragonfly
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: kubernetes
  source:
    repoURL: ghcr.io/dragonflydb
    chart: dragonfly/helm/dragonfly
    targetRevision: v1.30.3
{{/*    helm:*/}}
{{/*      valuesObject:*/}}
  destination:
    name: in-cluster
    namespace: database
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true