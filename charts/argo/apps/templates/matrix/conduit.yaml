apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: conduit
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: kubernetes
  source:
    repoURL: https://github.com/m00nwtchr/conduwuit-helm-chart.git
    path: .
    targetRevision: HEAD
    helm:
      valuesObject:
        image:
          repository: forgejo.ellis.link/continuwuation/continuwuity
          tag: main@sha256:eb956eb5956b9d78fdf0835d7d0c19dc012a7478c87419fa9b36a1e39e20dae1
          pullPolicy: IfNotPresent

        config:
          server_name: {{ .Values.domain }}

        ingress:
          enabled: true
          annotations:
            traefik.ingress.kubernetes.io/router.tls.options: traefik-intermediate@kubernetescrd

        resources:
          limits:
            memory: "4Gi"


  destination:
    name: in-cluster
    namespace: matrix
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true