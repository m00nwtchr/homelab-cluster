apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mautrix-discord
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: kubernetes
  source:
    repoURL: {{.Values.repoUrl}}
    path: charts/apps/mautrix-bridge
    targetRevision: HEAD
    helm:
      valuesObject:
        image:
          repository: dock.mau.dev/mautrix/discord
          tag: v0.7.3@sha256:852f900a2d326de1894ed8f1f6b22dbc2cc328e4e01ba1065ba32170e63e0ae8

        matrix:
          url: http://conduit.matrix.svc
          serverName: {{ .Values.domain }}

        bridge:
          type: discord

  destination:
    name: in-cluster
    namespace: matrix
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mautrix-discord
  namespace: matrix
spec:
  rules:
    - host: mautrix-discord.{{ .Values.domain }}
      http:
        paths:
          - backend:
              service:
                name: mautrix-discord
                port:
                  name: http
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - mautrix-discord.{{ .Values.domain }}