apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  knot.conf: {{ .Values.dnsConfig.config | nindent 2 }}
  example.zone: {{ .Values.dnsConfig.zones.example.com | nindent 2 }}
