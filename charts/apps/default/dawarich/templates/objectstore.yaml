---
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: dawarich-garage-store
  namespace: postgres
spec:
  configuration:
    destinationPath: s3://dawarich-postgres/
    endpointURL: http://garage.garage.svc:3900
    s3Credentials:
      accessKeyId:
        name: dawarich-garage
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: dawarich-garage
        key: ACCESS_SECRET_KEY
      region:
        name: garage
        key: REGION
    wal:
      compression: zstd
    data:
      compression: gzip
