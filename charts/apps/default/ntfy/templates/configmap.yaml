{{- if .Values.config.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: ntfy-config
data:
  server.yml: |
    {{- range $key, $value := .Values.config.data }}
      {{ $key }}: {{ $value }}
    {{- end }}

{{- end }}
