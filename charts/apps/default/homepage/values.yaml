---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/app-template-3.2.0/charts/other/app-template/values.schema.json

app-template:
  defaultPodOptions:
    securityContext:
      seccompProfile:
        type: RuntimeDefault

  controllers:
    homepage:
      strategy: Recreate
      annotations:
        reloader.stakater.com/auto: "true"

      containers:
        app:
          image:
            repository: ghcr.io/gethomepage/homepage
            tag: v1.2.0@sha256:5e6bcbb595c4e857540577135e0f8b3ffc2e97c23d6e0ddc7df183de5bfdd442
          env:
            TZ: "Europe/Warsaw"
#            LOG_LEVEL: debug
            HOMEPAGE_ALLOWED_HOSTS: "homepage.m00nlit.dev"
#          envFrom:
#            - secretRef:
#                name: &app homepage
          resources:
            requests:
              cpu: 25m
              memory: 105M
            limits:
              memory: 225M
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
      serviceAccount:
        identifier: *app

  rbac:
    roles:
      homepage:
        type: ClusterRole
        rules:
          - apiGroups:
              - ""
            resources:
              - namespaces
              - pods
              - nodes
            verbs:
              - get
              - list
          - apiGroups:
              - extensions
              - networking.k8s.io
            resources:
              - ingresses
            verbs:
              - get
              - list
          - apiGroups:
              - metrics.k8s.io
            resources:
              - nodes
              - pods
            verbs:
              - get
              - list
          - apiGroups:
              - apiextensions.k8s.io
            resources:
              - customresourcedefinitions/status
            verbs:
              - get
              - list
    bindings:
      homepage:
        type: ClusterRoleBinding
        roleRef:
          identifier: *app
        subjects:
          - identifier: *app

  serviceAccount:
    homepage: {}

  service:
    app:
      controller: *app
      ports:
        http:
          port: 80
          targetPort: 3000

#  ingress:
#    app:
#      hosts:
#        - host: "home.svc.m00nlit.dev"
#          paths:
#            - path: /
#              pathType: Prefix
#              service:
#                identifier: app
#                port: http

  persistence:
    config:
      type: configMap
      name: *app
      globalMounts:
        - path: /app/config/