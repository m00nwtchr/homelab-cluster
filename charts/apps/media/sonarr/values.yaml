sonarr:
  service:
    main:
      ports:
        http:
          port: 80
          targetPort: 8989

  ingress:
    main:
      enabled: true
      hosts:
        - host: sonarr.svc.m00nlit.dev
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - sonarr.svc.m00nlit.dev

  persistence:
    media:
      enabled: true
      type: pvc
      mountPath: /media
      existingClaim: media

  resources:
    limits: { }
    requests:
      cpu: 100m
      memory: 700Mi
