jellyfin:

  workload:
    main:
      podSpec:
        containers:
          main:
            env:
              JELLYFIN_PublishedServerUrl: https://jellyfin.m00nlit.dev
  
  ingress:
    main:
      enabled: true
      #      annotations:
      #        traefik.ingress.kubernetes.io/router.middlewares: traefik-vpn-only@kubernetescrd
      hosts:
        - host: jellyfin.m00nlit.dev
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - jellyfin.m00nlit.dev

  persistence:
    config:
      size: 1Gi
    media:
      enabled: true
      type: pvc
      mountPath: /media
      existingClaim: media

  resources:
    limits: { }
    requests:
      cpu: 100m
      memory: 700Mi
