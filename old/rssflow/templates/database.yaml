apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: postgres-{{ .Release.Name }}
  namespace: postgres
spec:
  name: {{ .Release.Name }}
  owner: {{ .Release.Name }}
  cluster:
    name: postgres
---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: postgres-{{ .Release.Name }}-websub
  namespace: postgres
spec:
  name: {{ .Release.Name }}-websub
  owner: {{ .Release.Name }}
  cluster:
    name: postgres