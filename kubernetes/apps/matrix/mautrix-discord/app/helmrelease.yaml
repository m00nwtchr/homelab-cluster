---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app ${APP}
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    defaultPodOptions:
      securityContext:
        seccompProfile:
          type: RuntimeDefault

    controllers:
      main:
        type: statefulset
        annotations:
          reloader.stakater.com/auto: "true"

        pod:
          securityContext:
            runAsUser: 1337
            runAsGroup: 1337
            runAsNonRoot: true
            fsGroup: 1337
            fsGroupChangePolicy: "OnRootMismatch"

        containers:
          app:
            image:
              repository: dock.mau.dev/mautrix/discord
              tag: v0.7.4@sha256:d1a4e86b5e0945dcf8a5332d6cf7a56c49b8e1d087590a04150a4f8af588a1cb
            command: [ "/usr/bin/${APP}" ]
            args: [ "-c", "/config/config.yaml", "--no-update" ]
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /_matrix/mau/live
                    port: &port 8000
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness:
                <<: *probe
                spec:
                  httpGet:
                    path: /_matrix/mau/ready
                    port: *port
            resources:
              limits:
                # cpu: 50M
                memory: 256Gi
              requests:
                cpu: 50m
                memory: 128Mi
            securityContext:
              capabilities:
                drop:
                  - ALL

    service:
      app:
        controller: main
        publishNotReadyAddresses: true
        ports:
          http:
            port: 80
            targetPort: *port

    route:
      app:
        hostnames: [ "{{ .Release.Name }}.${SECRET_DOMAIN}" ]
        parentRefs:
          - name: external
            namespace: envoy-gateway-system
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: 80

    persistence:
      data:
        size: 1Gi
        accessMode: ReadWriteOnce
      #        enabled: true
      #        type: persistentVolumeClaim
      #        existingClaim: *app

      config:
        type: secret
        name: ${APP}-config
        globalMounts:
          - path: /config/config.yaml
            subPath: config.yaml
