---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - externalsecret.yaml
  - helmrelease.yaml
  - secret.sops.yaml
configMapGenerator:
  - name: heisenbridge-configmap
    files:
      - resources/registration.yaml
configurations:
  - ./resources/kustomizeconfig.yaml
