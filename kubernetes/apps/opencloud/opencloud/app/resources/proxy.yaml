role_assignment:
  driver: oidc
  oidc_role_mapper:
    role_claim: groups
    role_mapping:
      - role_name: admin
        claim_value: admins@idm.${SECRET_DOMAIN}
      - role_name: user
        claim_value: users@idm.${SECRET_DOMAIN}
      - role_name: guest
        claim_value: myGuestRole
