---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: dragonfly-stunnel
    files:
      - ../../../../components/dragonfly/base/proxy/stunnel.conf
    options:
      disableNameSuffixHash: true
resources:
  - ./helmrelease.yaml
  - ./pvc.yaml
  - ./rbac.yaml
  - ./secret.sops.yaml
  - ./tcproute.yaml
