---
# yaml-language-server: $schema=https://k8s-schemas.m00nlit.dev/gateway.networking.k8s.io/listenerset_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: internal
spec:
  parentRef:
    name: internal
    namespace: network
  listeners:
    - name: ssh
      protocol: TCP
      port: 22
---
# yaml-language-server: $schema=https://k8s-schemas.m00nlit.dev/gateway.networking.k8s.io/listenerset_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: external
spec:
  parentRef:
    name: external
    namespace: network
  listeners:
    - name: ssh
      protocol: TCP
      port: 22
