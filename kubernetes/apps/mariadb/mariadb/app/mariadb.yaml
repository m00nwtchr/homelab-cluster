---
apiVersion: k8s.mariadb.com/v1alpha1
kind: MariaDB
metadata:
  name: mariadb
spec:
  rootPasswordSecretKeyRef:
    name: mariadb
    key: password
    generate: true
  storage:
    size: 10Gi
  service:
    type: ClusterIP
  tls:
    enabled: false
