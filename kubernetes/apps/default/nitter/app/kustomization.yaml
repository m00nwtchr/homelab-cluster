---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./secret.sops.yaml
configMapGenerator:
  - name: nitter-configmap
    files:
      - ./resources/nitter.conf
configurations:
  - ./resources/kustomizeconfig.yaml
