---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app ${APP}
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      renovate:
        type: cronjob
        cronjob:
          schedule: "@hourly"

        pod:
          securityContext:
            supplementalGroups:
              - 991 # TPM TSS

        containers:
          app:
            image:
              repository: ghcr.io/renovatebot/renovate
              tag: 41.62.4@sha256:497f067ac22e501a0f53fcd41dd6e066b41eee39b7520df7920b934fa76e76a8
              pullPolicy: IfNotPresent
            envFrom:
              - secretRef:
                  name: renovate-secret
            env:
              LOG_LEVEL: debug
              RENOVATE_PLATFORM: 'gitea'
              RENOVATE_ENDPOINT: 'https://git.${SECRET_DOMAIN}/api/v1'
              RENOVATE_AUTODISCOVER: 'true'
              RENOVATE_GIT_AUTHOR: 'Renovate <renovate@${SECRET_DOMAIN}>'
              RENOVATE_CONFIG_FILE: '/opt/renovate/config.json'
            resources:
              requests:
                cpu: 250m
#                memory: 500Mi
#                squat.ai/tpmrm: 1
              limits:
                cpu: '2'
#                memory: 4Gi
#                squat.ai/tpmrm: 1
    #  service:
    #    renovate:
    #      controller: renovate
    #      ports:
    #        http:
    #          port: 8080
    persistence:
      config:
        enabled: true
        type: configMap
        name: renovate-configmap
        globalMounts:
          - path: /opt/renovate/config.json
            subPath: config.json
      cache:
        enabled: true
        type: emptyDir
        globalMounts:
          - path: /tmp/renovate
    #  ingress:
    #    renovate:
    #      enabled: true
    #      hosts:
    #        - host: "renovate.svc.${SECRET_DOMAIN}"
    #          paths:
    #            - path: /
    #              service:
    #                identifier: renovate
    #                port: http
