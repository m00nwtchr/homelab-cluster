---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ${APP}-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: k8s-store-postgres-ns
  target:
    name: ${APP}-secret
    template:
      engineVersion: v2
      data:
        HASS_RECORDER_DB_URL: postgresql://{{ .username }}:{{ .password }}@pooler-rw.postgres.svc/${APP}
  dataFrom:
    - extract:
        key: postgres-${APP}
