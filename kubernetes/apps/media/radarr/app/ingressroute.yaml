---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: &app ${APP}
  namespace: media
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Media
    gethomepage.dev/name: Radarr
    gethomepage.dev/href: "https://radarr.svc.m00nlit.dev"
    gethomepage.dev/icon: ${APP}.svg
    gethomepage.dev/widget.type: *app
    gethomepage.dev/widget.enableQueue: "true"
    gethomepage.dev/widget.url: "http://${APP}.media.svc"
    gethomepage.dev/widget.key: "{{HOMEPAGE_VAR_RADARR_API_KEY}}"
spec:
  routes:
    - match: 'Host(`${APP}.svc.m00nlit.dev`)'
      kind: Rule
      middlewares:
        - name: vpn-only
          namespace: traefik
        - name: auth
          namespace: traefik
      services:
        - name: *app
          port: http
    - match: 'Host(`${APP}.svc.m00nlit.dev`) && PathPrefix(`/api`)'
      kind: Rule
      middlewares:
        - name: vpn-only
          namespace: traefik
      services:
        - name: *app
          port: http
