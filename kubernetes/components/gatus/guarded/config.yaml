---
endpoints:
  - name: "${APP}"
    group: guarded
    url: 1.1.1.1
    interval: 1m
    ui:
      hide-hostname: true
      hide-url: true
    dns:
      query-name: "${DOMAIN:=${SUBDOMAIN:=${APP}}.${SECRET_DOMAIN}}"
      query-type: AAAA
    conditions:
      - len([BODY]) == 0
